Feature: User can be retrieved
  Scenario: client makes call to GET /find-one
    When the client calls /find-one
    Then the client receives status code of 200
    And the client receives User Tiara

